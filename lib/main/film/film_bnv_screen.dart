import 'package:flutter/material.dart';

class FilmBnvScreen extends StatelessWidget {
  const FilmBnvScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Hello, Film!")
      ),
    );
  }

}