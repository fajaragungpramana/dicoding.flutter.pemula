import 'package:flutter/material.dart';

class TelevisionBnvScreen extends StatelessWidget {
  const TelevisionBnvScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Hello, Television!")
      ),
    );
  }

}